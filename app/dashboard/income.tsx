import { BaseText, BaseScrollView } from "@guden-components"

export const IncomeScreen = () => {
    return (
        <BaseScrollView>
            <BaseText text='Income' />
        </BaseScrollView>
    )
}